import { styled } from "$/styled-system/jsx";

// Headings
export const Body = styled("body", {
  base: {
    fontFamily: "body",
    fontSize: "body",
    lineHeight: "body",
    marginBottom: "6",
  },
});
